// Used automatically for production builds (`ng build`). Also relative,
// since in production the Angular app and API are typically served
// behind the same reverse proxy / domain - no hardcoded host needed.
export const environment = {
  production: true,
  apiUrl: '/api/v1',
};
