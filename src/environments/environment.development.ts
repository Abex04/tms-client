// Used automatically when running `ng serve` (development builds).
// apiUrl is relative ('/api/v1') rather than absolute (e.g.
// 'http://localhost:5189/api/v1') so it works through the dev-server
// proxy (proxy.conf.json) without hardcoding a port number here.
export const environment = {
  production: false,
  apiUrl: '/api/v1',
};
